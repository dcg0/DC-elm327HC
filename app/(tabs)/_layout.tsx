import { Tabs } from "expo-router";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { Platform } from "react-native";
import { HapticTab } from "@/components/haptic-tab";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { useColors } from "@/hooks/use-colors";
export default function TabLayout() { const colors = useColors(); const insets = useSafeAreaInsets(); const bottom = Platform.OS === "web" ? 10 : Math.max(insets.bottom, 8); return <Tabs screenOptions={{ headerShown: false, tabBarActiveTintColor: "#29D3C5", tabBarInactiveTintColor: colors.muted, tabBarButton: HapticTab, tabBarStyle: { paddingTop: 8, paddingBottom: bottom, height: 58 + bottom, backgroundColor: "#0E1B2C", borderTopColor: "#223B56", borderTopWidth: 1 } }}><Tabs.Screen name="index" options={{ title: "Tablero", tabBarIcon: ({ color }) => <IconSymbol size={23} name="gauge" color={color} /> }} /><Tabs.Screen name="sensors" options={{ title: "Sensores", tabBarIcon: ({ color }) => <IconSymbol size={23} name="sensor" color={color} /> }} /><Tabs.Screen name="pairing" options={{ title: "Emparejar", tabBarIcon: ({ color }) => <IconSymbol size={23} name="paperplane.fill" color={color} /> }} /><Tabs.Screen name="dtc" options={{ title: "DTC", tabBarIcon: ({ color }) => <IconSymbol size={23} name="exclamationmark.triangle" color={color} /> }} /><Tabs.Screen name="settings" options={{ title: "Ajustes", tabBarIcon: ({ color }) => <IconSymbol size={23} name="gearshape" color={color} /> }} /></Tabs>; }
